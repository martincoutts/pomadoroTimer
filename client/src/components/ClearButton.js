import React, { Component } from "react";

export default class ClearButton extends Component {
  render() {
    return (
      <div id="clear-button">
        <button>Clear</button>
      </div>
    );
  }
}
